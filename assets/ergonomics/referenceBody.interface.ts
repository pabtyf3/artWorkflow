export interface ReferenceBody {
  standingHeight: number
  kneeHeight: number
  hipHeight: number

  standingEyeHeight: number
  seatedEyeHeight: number

  /**
   * Seated elbow height (neutral posture)
   */
  seatedElbowHeight: number

  shoulderWidth: number
  armLength: number
}
